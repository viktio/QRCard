use qrc;

create table users (
username varchar(50),
password varchar(50),
created datetime,
	last_login datetime
	);

create table 
QRDATA
(
username varchar(50),
name varchar(200),
surname varchar(200),
phone_no varchar(50),
mail varchar(100),
picture_name varchar(50),
qrcode varchar(200)
);

